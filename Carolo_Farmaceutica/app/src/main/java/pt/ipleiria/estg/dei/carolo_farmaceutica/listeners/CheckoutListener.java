package pt.ipleiria.estg.dei.carolo_farmaceutica.listeners;

public interface CheckoutListener {

    // Método que é chamado quando o checkout é feito
    void onRefreshCheckout(boolean resposta);
}
